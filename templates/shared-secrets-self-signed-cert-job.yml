{{- $sharedSecretValues := index .Values "shared-secrets" -}}
{{- if and $sharedSecretValues.enabled (not .Values.global.operator.enabled) }}
{{ include (print $.Template.BasePath "/_shared-secrets-self-signed-cert-job.yml") . }}
{{- end }}
