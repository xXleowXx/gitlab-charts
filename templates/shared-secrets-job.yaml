{{ $sharedSecretEnabled := index .Values "shared-secrets" "enabled" }}
{{- if and $sharedSecretEnabled (not .Values.global.operator.enabled) }}
{{ include (print $.Template.BasePath "/_shared-secrets-jobspec.yaml") . }}
{{- end }}
