{{- if and (.Values.global.serviceAccount.enabled) (not .Values.global.serviceAccount.name) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "fullname" . }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "gitlab.standardLabels" . | nindent 4 }}
{{- end }}